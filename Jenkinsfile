#!groovy


pipeline {
    agent any
    options {
        timestamps ()
        }
    stages {
        stage("first_step") {
            steps {
                sh 'echo "hello_world"'
                }
        }
    }
}
